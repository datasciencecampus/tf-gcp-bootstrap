# Minimal Example

This example shows the smallest intended use of the module.

It grants a host CI service account permission to impersonate a pre-existing target Terraform service account and applies only the module's default project-level roles.

Replace the placeholder project ID and service account emails before use.
